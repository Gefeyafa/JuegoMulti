text_id = "";

// Initialize the sprite to Link_Walk_Right
sprite_index = Link_Walk_Right;

// Set the alarm[0] to trigger after 3 seconds (3 seconds * room_speed frames per second)
alarm[0] = room_speed * 1;